{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf disd && cd ../fso-note-demo-frontend && pnpm run build && cp -r dist ../fso-note-demo-backend",
    "deploy:full": "pnpm run build:ui && git add . && git commit -m 'UI build' && git push"
  },
  "author": "Filip Madunicky",
  "license": "MIT",
  "packageManager": "pnpm@10.15.0",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
